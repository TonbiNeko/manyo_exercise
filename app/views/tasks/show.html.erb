<h1>タスク詳細</h1>
<p>タスク名:<%= @task.name %></p>
<p>タスク内容：<%= @task.description %><p>

<%= link_to '編集', edit_task_path(@task.id) %>
<%= link_to '削除', task_path(@task.id), method: :delete, data: { confirm: '削除してよろしいですか？' } %>
<br>
<%= link_to 'タスク一覧に戻る', tasks_path %>



